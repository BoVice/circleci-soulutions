version: 2
jobs:
  build:
    machine:
      image: circleci/classic:edg
      environment:
        http_proxy: lalala
        https_proxy: blablahblah
        no_proxy: boop
        AWS_DEFAULT_REGION: us-east-1
    steps:
        - checkout
        - echo "Hello world"